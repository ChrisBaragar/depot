<!--This is actually real cool! It switches the css id of the line item depending on if it's the recently created one.-->

<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
<tr>
<% end %>
	<td><%= line_item.quantity %>&times;</td>
	<td><%= line_item.product.title %></td>
	<td class="item_price"><%= number_to_currency(line_item.total_price) %></td>
	<td><%= button_to 'X', decrement_line_item_path(line_item), method: :put, remote: true %></td>
</tr>